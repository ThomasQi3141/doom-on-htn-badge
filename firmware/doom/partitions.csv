# Doom on the badge.
#
# The stock 2.6 MB Lua app is gone and its space becomes WAD storage. Sizing is
# driven by one result: with every sound, every piece of music, ENDOOM and the
# attract-mode demos removed, the shareware IWAD is 3,362,373 bytes -- so it
# fits here uncompressed, and no recompressed WAD format is needed.
#
# The app was 640 KB until the radio arrived: the Wi-Fi driver, PHY and
# supplicant add roughly 290 KB of code, so the app partition is now 1 MB.
# That still leaves the WAD partition at 3,080,192 bytes -- the arena WAD
# mkarena.py builds is 2,591,387 bytes, so 477 KB is spare -- but the full
# stripped shareware IWAD (3,362,373 bytes) no longer fits alongside it.
#
# Name,     Type, SubType,  Offset,    Size
nvs,        data, nvs,      0x9000,    0x4000
phy_init,   data, phy,      0xd000,    0x1000
factory,    app,  factory,  0x10000,   0x100000
wad,        data, 0x40,     0x110000,  0x2F0000
